#include <stdio.h>
void selam_soyle() {
   printf("merhaba");
}
int main() {
   // printf() displays the string inside quotation
   int a = 3;
   int b = 2;
   
   if(a == b ) {
      printf("a ve b birbirine eşittir.");
      printf("a ve b birbirine eşittir.");
      printf("a ve b birbirine eşittir.");
   }
   switch (a) {
      case 1 :
         printf("daf"); break;
      case 2 :
         printf("dsafdsa"); break;
      default :
         print("dfasfads")
   }
   if( a == 2 && b == 3 || a == b){

   }
   for (int index = 0; index < 10 ; index ++ ) {
      printf("Hello, World!");
   }
   int index = 0;
   while(index<10) {
      index += 2;
   }
   printf("index no:%d",index);
   return 0;
}
